
---

```bash
mkdir -p /opt/docker/myspeed/data
```

```bash
nano /opt/docker/myspeed/docker-compose.yml
```

```yaml
services:
  myspeed:
    image: germannewsmaker/myspeed:latest
    container_name: myspeed
    restart: unless-stopped

    ports:
      - "5216:5216"

    volumes:
      - ./data:/myspeed/data

    networks:
      - services

networks:
  services:
    external: true
```

```bash
cd /opt/docker/myspeed
```

```bash
docker compose up -d
```

---

# Heimdall

```bash
mkdir -p /opt/docker/heimdall/config
```

```bash
nano /opt/docker/heimdall/docker-compose.yml
```

```bash
services:
  heimdall:
    image: lscr.io/linuxserver/heimdall:latest
    container_name: heimdall
    restart: unless-stopped

    ports:
      - "8080:80"

    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Sao_Paulo

    volumes:
      - ./config:/config

    networks:
      - services

networks:
  services:
    external: true
```

```bash
cd /opt/docker/heimdall
```

```bash
docker compose up -d
```

---

# Uptime-kuma

```bash
mkdir -p /opt/docker/uptime-kuma/data
```

```bash
nano /opt/docker/uptime-kuma/docker-compose.yml
```

```bash
services:
  uptime-kuma:
    image: louislam/uptime-kuma:latest
    container_name: uptime-kuma
    restart: unless-stopped

    ports:
      - "3001:3001"

    volumes:
      - ./data:/app/data

    networks:
      - services

networks:
  services:
    external: true
```

```bash
cd /opt/docker/uptime-kuma
```

```bash
docker compose up -d
```

---

# Home-Assistant

```bash
mkdir -p /opt/docker/homeassistant/config
```

```bash
nano /opt/docker/homeassistant/docker-compose.yml
```

```bash
services:
  homeassistant:
    image: ghcr.io/home-assistant/home-assistant:stable
    container_name: homeassistant
    restart: unless-stopped

    network_mode: host

    environment:
      TZ: America/Sao_Paulo

    volumes:
      - ./config:/config
      - /etc/localtime:/etc/localtime:ro

networks:
  services:
    external: true
```

```bash
cd /opt/docker/homeassistant
```

```bash
docker compose up -d
```

---

# Dozzle

```bash
mkdir -p /opt/docker/dozzle
```

```bash
nano /opt/docker/dozzle/docker-compose.yml
```

```bash
services:
  dozzle:
    image: amir20/dozzle:latest
    container_name: dozzle
    restart: unless-stopped

    ports:
      - "8081:8080"

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro

    networks:
      - services

networks:
  services:
    external: true
```

```bash
cd /opt/docker/dozzle
```

```bash
docker compose up -d
```

---
